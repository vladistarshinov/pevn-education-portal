{
  "name": "pevn-education-portal",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "server": "cd server/ && npm run start",
    "client": "cd client/ && npm run serve",
    "install_modules": "npm i && cd client/ && npm i && cd ../server && npm i && cd ../",
    "app": "npm-run-all --parallel server client"
  },
  "main": "app.js",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
